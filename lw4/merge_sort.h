#pragma once

#include <iostream>
#include <vector>
#include <string>

#include "TRecord.h"

void merge_sort(std::vector<TRecord>&, size_t, size_t);
void merge_sorting(std::vector<TRecord>&, size_t, size_t, size_t);
